{
	"name": "@checkr",
	"version": "0.1.0",
	"type": "module",
	"private": true,
	"dependencies": {
		"chalk": "^5.4.1"
		},
	"devDependencies": {
		"vitest": "catalog:"
	},
	"scripts": {
		"test": "vitest run",
		"check": "node check-all.js",
		"build": "echo 'No build needed for @symphony/toolkit'",
		"typecheck": "echo 'No typecheck needed for @symphony/toolkit'"
	}
}
