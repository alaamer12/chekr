/** Public engine API — re-exported from the bundled internal @checkr/core package. */
export {
  ConfigError,
  ENGINE_DEFAULTS,
  loadConfig,
  resolveConfig,
  run,
  validateConfig,
} from "@checkr/core";
