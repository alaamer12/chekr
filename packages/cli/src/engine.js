/** Public engine API — re-exported from the bundled internal @chekr/core package. */
export {
  ConfigError,
  ENGINE_DEFAULTS,
  loadConfig,
  resolveConfig,
  run,
  validateConfig,
} from "@chekr/core";
