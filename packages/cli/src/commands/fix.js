/**
 * Stub — fix command ships post-MVP (plan.md Phase 5).
 * @param {Record<string, string | boolean>} _flags
 * @param {string[]} _positionals
 * @param {string} _cwd
 */
export async function fixCommand(_flags, _positionals, _cwd) {
  console.error("chekr fix is not yet implemented.");
  process.exitCode = 1;
}
